# Portfolio Ideas

## UI / Visual Changes
- Dark/light theme toggle (persist preference in localStorage) — O
- Smooth scroll-triggered fade-in/slide-in animations for sections — O
- Parallax scrolling effect on hero section — O
- Micro-interactions / hover effects on project cards (scale, glow, tilt) — O
- Animated typing effect for tagline in hero — O
- Staggered reveal for skill tags and project grid items — O

## Navigation & Layout
- Section navigation with progress indicator (scroll spy sidebar) — O
- Breadcrumb trail for blog posts — O
- Multi-page layout with React Router (instead of single-page scroll) — O
- Tab-based sub-navigation within Projects (e.g. "All", "Web", "Systems", "AI") — O


## Content / Sections
- Dedicated Skills page with progress bars or radar chart — X
- Experience / Work History timeline component — X
- Testimonials or shout-outs from colleagues — X
- Open Source contributions feed (GitHub API integration) — O
- Certifications or courses completed section — X
- Photography / gallery section — X
- Reading list / currently learning section — X
- Guestbook or visitor comments — O


## Blog Enhancements
- Individual post pages with permalinks (instead of accordion) — O
- RSS / Atom feed generation — O
- Social share buttons for blog posts — O
- Code syntax highlighting in markdown (rehype-highlight or Prism) — O

## Projects Enhancements
- Live demo embed (iframes or video walkthrough) — X
- GitHub stars / last commit date via API — O
- Before/after image comparison for visual projects — X


## Performance & SEO
- Image optimization pipeline (responsive srcset, WebP conversion) — O
- Preload hero image for faster LCP — O
- Meta tags and Open Graph previews for social sharing — O
- Sitemap generation — O
- Structured data (JSON-LD) for personal portfolio schema — O
- Service worker for offline support — O
- Font subsetting to reduce bundle size — O

## Accessibility


## Developer Experience
- Component storybook for UI development — O
- Automated visual regression tests (Playwright or Chromatic) — O
- Pre-commit hooks for formatting and linting — O
- CSS modules or Tailwind CSS migration — O
- TypeScript migration — O
- Unit tests for utility functions and components — O
- CI/CD preview deploys for pull requests — O
- Markdown validation for blog posts (required front-matter fields) — O
- Content management UI (headless CMS integration) — O
